workspace(name = "manifesto")

load("@manifesto//bazel:repos.bzl", "manifesto_repositories")
manifesto_repositories()

load("@manifesto//bazel:deps.bzl", "manifesto_dependencies")
manifesto_dependencies()
